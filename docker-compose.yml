version: '3.8'

services:
  frontend:
    build: ./betting-app
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend
    environment:
      - MONGO_URI=mongodb+srv://Bk:nRXAc9UohBD5vA7R@chat-app.bn1ewik.mongodb.net/?retryWrites=true&w=majority&appName=Chat-app
    ports:
      - "3000:3000"
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
